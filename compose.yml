services:
  web:
    container_name: tuning-visualizer
    build:
      context: .
    image: tuning-visualizer:latest
    ports:
      - "8080:8080" # host:container (rootless-safe)
    restart: unless-stopped